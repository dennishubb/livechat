<?php

    define('ADMIN_VERSION', 'update whenever deploy');

    // $html = file_get_contents(ROOT.'/admin/init.html');
    // $html = str_replace('{Version}', 'testing', $html);
    // $html = str_replace('{Root}', $ROOT, $html);

    // exit($html);
?>